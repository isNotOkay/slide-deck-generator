services:
  slide-deck:
    build: .
    image: slide-deck-generator
    ports:
      - "127.0.0.1:${SLIDE_DECK_PORT:-3001}:3001"
    volumes:
      - deck-data:/app/data
    restart: unless-stopped

volumes:
  deck-data:
